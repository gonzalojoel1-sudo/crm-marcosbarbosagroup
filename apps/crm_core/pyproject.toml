[project]
name = "crm_core"
version = "0.0.1"
description = "Premium CRM core — DocTypes + Google Calendar 2-way sync + Cal.com bridge"
readme = "README.md"
requires-python = ">=3.10,<3.15"
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=15.0.0,<16.0.0"

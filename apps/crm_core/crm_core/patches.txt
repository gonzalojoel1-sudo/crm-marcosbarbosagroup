# Frappe parsea este archivo como INI y exige la seccion que pide cada fase del
# migrate: sin `[pre_model_sync]` (aunque este vacia) `bench migrate` muere con
# "Patch type PatchType.pre_model_sync not found in patches.txt".
[pre_model_sync]
# Sin patches: todo lo de abajo corre despues del sync de DocTypes.

# F1: las reuniones pasan de `CRM Lead.custom_meeting_datetime` a `Event` linkeado
# por `Event.custom_crm_lead`. Va en post_model_sync: primero crea el Custom Field
# y después backfillea. Idempotente (se puede re-ejecutar sin duplicar).
[post_model_sync]
crm_core.patches
# F2: entrada propia para que la reconciliación del id de Google corra también en
# los sitios donde el patch de F1 ya quedó en `Patch Log` (no se re-ejecuta).
crm_core.patches_f2
# Categoría de la reunión: crea `Event.custom_crm_categoria` y backfillea el
# default en los `Event` existentes. Entrada propia por el mismo motivo que F2.
crm_core.patches_categoria
# S1 (sync con Google Calendar): crea los Custom Fields de estado por evento y
# backfillea el estado inicial. Entrada propia: los patches anteriores ya están
# en `Patch Log` y no se re-ejecutan.
crm_core.patches_gcal
